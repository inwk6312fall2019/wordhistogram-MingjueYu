#1
def rotate_word(word):
  index = 0
  pair = 0
  while index < len
  
#2
def duplicate_words(file):
  file = open("2600-0.txt")
  for line in file:
    word = line.strip()
    new_list = list(word)
  for ele in new_list:
    index = 0
    if ele[index] == ele[index+1]
      return True
      index = index + 1
  returen False


  
